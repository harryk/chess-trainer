{
  "name": "@chess-trainer/shared",
  "version": "1.0.0",
  "description": "Shared types, constants, and utilities for chess trainer",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "lint": "eslint src --ext .ts,.tsx",
    "lint:fix": "eslint src --ext .ts,.tsx --fix"
  },
  "devDependencies": {
    "typescript": "^5.3.2",
    "vitest": "^1.0.4"
  }
}
